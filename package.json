{
  "name": "mysterymirror-landpage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc --noEmit && vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "lucide": "^0.468.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "wrangler": "^4.105.0"
  }
}
